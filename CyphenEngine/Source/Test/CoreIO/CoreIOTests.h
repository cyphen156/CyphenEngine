#pragma once

// ============================================================================
// CoreIoTests
// ----------------------------------------------------------------------------
// Debug 전용 Core I/O 테스트 진입점.
//
// #1_12 / #1_13에서 닫힌 Core Path / TextCodec / File 계약을 회귀 가드로 고정한다.
// Launch가 엔진 스레드 시작 전(메인 스레드)에서 _DEBUG 게이트로 호출한다.
//
// 규칙:
//     평평한 테스트 함수 + 작은 expect/count 만 사용한다.
//     테스트 등록기 / 리플렉션 / xUnit 류 프레임워크화 금지.
//     결과는 OutputDebugString으로만 출력하며, 실패해도 호출 흐름을 끊지 않는다.
//     실패 사유를 File / TextCodec API에 추가하지 않는다(테스트 단계 분리로만 구별).
// ============================================================================

void RunCoreIoTests();
